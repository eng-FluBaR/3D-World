# 🔧 ФИКСОВЕ - Качване и Навигация

## Какво поправих:

### 1. ✅ Storage RLS политики - ОБНОВЕНИ
- Пресъздадох admin политиките за да избегна "ambiguous column" грешка
- Всички политики сега работят правилно

### 2. ✅ Навигация - ПОДОБРЕНА
- Добавено детайлно логване за debug
- Използва се `.maybeSingle()` вместо `.single()`
- По-добро обработване на грешки

### 3. ✅ Auth съобщения - ПОДОБРЕНИ
- Детайлно логване на session status
- По-ясни съобщения за грешки

### 4. ✅ Upload грешки - ПО-ДОБРИ
- Показва точната причина за грешка
- Специално съобщение за RLS проблеми

---

## 🧪 МОЛЯ НАПРАВЕТЕ ТОВА (ВАЖНО):

### Стъпка 1: Изчистване на cache
```
1. Натиснете F12 (отваря Developer Tools)
2. Отидете в Application tab (горе)
3. В лявото меню: Storage → Clear site data
4. Кликнете "Clear site data" бутон
5. Затворете Developer Tools
6. Презаредете страницата с Ctrl+F5
```

### Стъпка 2: Излезте и влезте отново
```
1. Отидете на: http://localhost:5174/
2. Ако има някакво меню за изход - излезте
3. Отидете на: http://localhost:5174/login.html
4. Влезте с:
   - Email: test@abv.bg
   - Password: [вашата парола]
```

### Стъпка 3: Проверете Console логовете
```
Отворете Console (F12 → Console tab) и трябва да видите:

[AUTH] Session status: active aa291a53-...
[NAV] Initializing navigation for page: upload
[NAV] Session: exists
[NAV] Found user: test role: user
[NAV] User info: {role: "user", name: "test"}
[NAV] Is authenticated: true
[NAV] Is admin: false
```

### Стъпка 4: Проверете менюто
```
В горния десен ъгъл трябва да видите:
👤 test ▼
   Профил
   ━━━━━━
   Изход

НЕ трябва да виждате: "Вход" и "Регистрация"
```

### Стъпка 5: Опитайте да качите файл
```
1. Изберете .stl или .obj файл
2. Изберете материал
3. Кликнете "Изпрати заявка"
4. Трябва да видите: "Заявката е изпратена успешно"
```

---

## 🚨 АКО ВСЕ ОЩЕ ИМА ПРОБЛЕМИ:

### Проблем: Не вижда името в менюто
**Копирайте това в Console (F12) и изпратете резултата:**
```javascript
// Проверка на session
const { createSupabaseClient } = await import('/src/services/supabase.js');
const client = createSupabaseClient();
const { data: session } = await client.auth.getSession();
console.log('Session:', session?.session?.user?.id);

// Проверка на profile
if (session?.session?.user?.id) {
  const { data: profile, error } = await client
    .from('profiles')
    .select('*')
    .eq('user_id', session.session.user.id)
    .single();
  console.log('Profile:', profile, 'Error:', error);
}
```

### Проблем: database error code: 42P17
**Вече е поправено!** Изтрийте cache и влезте отново.

### Проблем: row-level security policy
**Решение:**
1. Излезте
2. Изчистете cache (Application → Clear site data)
3. Влезте отново
4. Опитайте пак

---

## 📊 SQL Промени които направих:

```sql
-- Пресъздадох admin политиките с alias за избягване на ambiguous column
DROP POLICY IF EXISTS "Admins can update requests" ON public.requests;
DROP POLICY IF EXISTS "Admins can view all requests" ON public.requests;

CREATE POLICY "Admins can update requests"
ON public.requests FOR UPDATE TO authenticated
USING (
  EXISTS (
    SELECT 1 FROM public.profiles p
    WHERE p.user_id = auth.uid()
    AND p.role IN ('moderator', 'super_admin')
  )
);

CREATE POLICY "Admins can view all requests"
ON public.requests FOR SELECT TO authenticated
USING (
  EXISTS (
    SELECT 1 FROM public.profiles p
    WHERE p.user_id = auth.uid()
    AND p.role IN ('moderator', 'super_admin')
  )
);
```

---

## ✅ След изчистване на cache и re-login всичко трябва да работи!

Ако все още има проблем, изпратете ми screenshot на Console логовете след login.
